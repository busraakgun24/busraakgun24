// Narrative Engine - İlk Prototip
const creator = "Büşra";
let state = "Writer sees the map, the player just lives.";

function initEngine() {
    console.log("Sistem Mimarisi Başlatılıyor...");
    console.log("Kurucu: " + creator);
    console.log("Motor Durumu: " + state);
}

// Sistemi çalıştır
initEngine();